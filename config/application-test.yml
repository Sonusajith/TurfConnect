# Centralized Spring Boot Testing Configuration (application-test.yml)

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/turfconnect_test
    redis:
      host: localhost
      port: 6379
      password:
  rabbitmq:
    listener:
      type: simple

# Minimal logging during tests
logging:
  level:
    root: WARN
    com.turfconnect: INFO
    org.springframework: WARN
